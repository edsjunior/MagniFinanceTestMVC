@model IEnumerable<MagniFinanceTest.MVC.ViewModels.StudentSubjectsViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CourseSubjectId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StudentId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Grade)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CourseSubjectId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StudentId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Grade)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StudentSubjectsId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StudentSubjectsId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StudentSubjectsId })
        </td>
    </tr>
}

</table>
